<div class="form-actions">
	<section>
        <div class="navbar navbar-inverse navbar-fixed-top">
            <div class="navbar-inner well-small">
                <div class=" container">
                    <a class="btn btn-navbar" data-toggle="collapse" data-target=".navbar-responsive-collapse">
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </a>
                    
                    <div class="nav-collapse collapse navbar-responsive-collapse">
                        <ul class="nav navbar-pills pull-right">
                                                     
     						<li class="<%= 'activen' if current_page?(root_path) %>">
	                            	<%= link_to "Home", root_path %>
	                        </li

	                        <li class="<%= 'activen' if current_page?(scrape_index_path) %>">
	                            	<%= link_to "Scrape", scrape_index_path %>
	                        </li>
							<% if signed_in? %>
							<li class="dropdown">
	                               <%= link_to "Manage Property", properties_path %>
	                            </li>

	                            <li class="dropdown">
	                               <%= link_to "My account", profile_path(current_user.id), :method => :get %>
	                            </li>
	                            <li class="dropdown">
	                               <%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => :offset1  %>
	                            </li>
	                        <% else %>

	                            <li class="dropdown">
	                                <%= link_to "About us", about_path %>
	                            </li>
	                            
	                            
	                             <li class="divider"><%= link_to "Sign in", new_user_session_path %></li>
	                             <li class="divider"><%= link_to "Sign up", new_user_registration_path %></li>

                            <% end %>
                          
                        </ul>
                    </div>

                    <div> 
                    	<a ><%= image_tag( "logo.png" , :width =>"4%", :class => "pull-left img-circle") %></a>	
                    	<a class="brand text-info">Property Connect </a>
                    	<i class="text-success"><%= current_user.email if user_signed_in? %></i>
                    	<i class="text-danger"><%= alert %></i>
                    	<i class="notice text-success"><%= notice %></i>

                    	 <%= form_tag(properties_path, :method => "get", class: "navbar-form pull-right", id: "search-form") do %>
					    <div class="input-append">
					      <%= text_field_tag :search, params[:search], class: "span2", placeholder: "Search Property" %> 
					      <!-- In order to have the "search" icon int the button, we need to use plain HTML instead 
					           of using a Rails form helper -->
					      <button class="btn" type="submit"><i class="icon-search"></i></button>
					    </div>
					  <% end %>

                </div>
            </div>
        </div>
    </section>
	<!--Tabbed menu-->

	<br/>
	<section >
		<div class="top-nav container">
			<div class="row">
				<div class="span2">
					
				</div>
				<div class="span4">
					<p>
						Your choice <small>(New properties on the market)</small>
					</p>
				</div>
				
				<div class="span3">
					
							<span class="text-right"></span>
							
							<p>
								 <%= link_to "Check out new Houses and Land", root_path, :class => :btn %>

							</p>
					
				</div>

				<div class="span3">
					<p>
						Manage<small> and find property</small>
					</p>
										
				</div>
			</div>
		</div>
	</section>
</div>